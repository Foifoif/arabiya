name = "arabiya"
compatibility_date = "2024-11-01"
pages_build_output_dir = "."

# D1 binding is configured in the Cloudflare Pages dashboard:
# Settings → Functions → D1 database bindings → DB → arabiya-db
